extends layout

block append scripts
  script(src='/scripts/calendar.js')
  script(src='/scripts/dropdown.js')
  script(src='/scripts/homescript.js')
  script(src='/scripts/weather.js')

block content
  .week
    p 7 Day Forcast and Predicted Mood
    
    .weekContainer
    <div class="week">Loading...</div>
      //- - let days=['M', 'Tu', 'W', 'Th', 'F', 'Sat', 'Sun'];
      //- each day in days
      //-   .weekItem
      //-     h1(id="list")= day
      //-     .weekItemContent
      //-       ul
      //-           li Name: #{user.first_name} #{user.last_name}
      //-           li Username: #{user.user_name}
      //-           li Email: #{user.email}
            
  .current
    h1(id="currentTemp")
    .currentWeather
      img(id="documentIconElement")
      p(id="weatherDescriptionElement")
  .moods
    .moodsModule
      h1 -30%
      p Your mood
    .moodsModule
      h1 -28%
      select(name="moodgroup" id="moodgroup")
        option(value="zip") Zip Code
  .calendarContainer
    <div class="calendar">HELLOOO</div>
    .calendarKey
      - let colors=['red', 'orange', 'yellow', 'greenyellow', 'forestgreen']
      - let label=['-61% to -100%', '-31% to -60%', '-30% to 30%', '31% to 60%', '61% to 100%']
      - let i = 0
      while i < 5
        .calendarKeyItem
          <div class="calendarKeyColor" style="background-color: #{colors[i]}"></div>
          p #{label[i]}
        - i++
